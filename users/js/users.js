// Reserved for future user-management interactivity (e.g. search/filter).
document.addEventListener('DOMContentLoaded', function () {
    // No dynamic behaviour required beyond native form submits at this time.
});
